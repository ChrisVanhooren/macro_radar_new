[build]
builder = "NIXPACKS"
buildCommand = "npm ci --cache /tmp/npm-cache && npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
